import styled from 'styled-components';

const MeetingContent = styled.main`
	flex: 1;
	height: calc(100% - var(--bottom-bar-height));
	padding: 1.2rem;
	display: flex;
`;

export default MeetingContent;
